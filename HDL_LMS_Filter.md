# HDL Coder 예제: HDL Implementation of LMS Filter

> 공식 예제: **"HDL Implementation of LMS Filter"**
> 목적: **완전직렬(Fully-Serial) LMS(최소 평균 제곱) 적응형 필터** 알고리즘을 하드웨어로 구현.

## 개요
**LMS(Least Mean Squares) 적응형 필터**를 하드웨어(FPGA/ASIC)용으로 구현합니다. LMS는 계수를 입력 신호에 따라 **적응적으로 갱신**하는 필터로, 노이즈 제거, 에코 제거, 채널 등화, 시스템 식별 등에 널리 사용됩니다. 이 예제는 **완전직렬(fully-serial)** 구조로 승산기 자원을 최소화합니다.

## 필요한 툴박스
- DSP HDL Toolbox
- HDL Coder
- Simulink
- Fixed-Point Designer

## LMS 알고리즘 개요
LMS는 원하는 신호와의 오차를 최소화하도록 필터 계수를 반복 갱신:

```
y[n]   = Σ w[k]·x[n-k]        // 필터 출력 (convolution)
e[n]   = d[n] - y[n]          // 오차 (원하는 신호 - 출력)
w[k]   = w[k] + μ·e[n]·x[n-k] // 계수 갱신 (μ: 스텝 크기, 학습률)
```
- **적응**: 계수가 매 샘플마다 오차 기반으로 갱신
- **수렴**: μ(학습률)에 따라 수렴 속도와 안정성 결정

## 하드웨어 구현 (완전직렬)
완전직렬(fully-serial) 구조:
- **1개의 승산기**를 시분할로 재사용
- 필터 출력 계산 → 오차 계산 → 계수 갱신을 하나의 승산기로 순차 처리
- 하드웨어 자원(승산기·로직) 최소화, 처리량은 낮음

### 하드웨어 블록 흐름
```
x[n] ──► [지연선(delay line)] ──► [승산기] ──► [누적] ──► y[n]
                                      ▲
                    (계수 w 갱신) ←── [오차/μ 처리]
```
- 지연선: 수신 신호의 과거 샘플 저장
- 승산기: 출력 계산 + 계수 갱신 양쪽에 공유
- μ(학습률): 고정소수점 상수로 구현

## 모델 구성
- LMS 필터: `LMS Filter` 블록(DSP HDL Toolbox) 또는 수동 블록으로 구성
- **고정소수점** 데이터 타입 (적응 필터는 고정소수점 양자화 오차에 민감 → 비트 폭 설정 중요)
- μ(스텝 크기)가 2의 거듭제곱이면 시프트 연산으로 하드웨어 단순화

## HDL 코드 생성
```matlab
makehdl('모델명/LMS_DUT');
```

## 검증
- 입력: 원하는 신호 + 노이즈
- LMS 필터 출력이 노이즈를 제거하며 원하는 신호로 **수렴**하는지 확인
- 수렴 곡선(오차 감소) 검증

## 장단점 정리
| 장점 | 단점 |
|------|------|
| 승산기·자원 최소화 (완전직렬) | 처리량 낮음 (직렬 처리) |
| 적응형(동적 계수) 지원 | 수렴 시간을 위한 제어 필요 |
| 에코/노이즈 제거 등 실용 응용 | 고정소수점 오차 민감 |

## 실행 요약
1. LMS 적응 필터 모델 구성 (완전직렬)
2. 고정소수점 타입과 μ(학습률) 설정
3. 노이즈 입력으로 수렴 검증
4. `makehdl`로 HDL 생성

## 참고
- 공식 문서: DSP HDL Toolbox → HDL Implementation of LMS Filter (Open Script)
- 연관: LMS Filter 블록 문서, FIR Filter Architectures, Adaptive Filters
